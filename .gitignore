# macOS
.DS_Store

# Claude Code
.claude/settings.local.json

# Node / Next.js
node_modules/
dashboard/.next/
dashboard/.vercel/
dashboard/.env*.local

# DB 백업 — 실제 GPS 궤적이 들어 있으므로 절대 커밋하지 않는다
backup/

# Flutter (app/ 내부는 app/.gitignore가 처리)
